<h1>Tiles</h1>
<div class="row">
<% @posts.each do |post| %>
<div class="col-md-3" align="center">
  <h3><%=post.title %></h3>
  <%= image_tag post.image.url %>

  <%= link_to "Detail",posts_path(post) %>
  <%= posts_path(post) %>

  <p><%= post.body %></p>
</div>
<%= link_to "Detail",posts_path(post) %>
<% end %>
	